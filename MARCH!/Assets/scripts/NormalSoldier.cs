using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NormalSoldier : SoldierMovement
{
    // Start is called before the first frame update
    void NewStart()
    {
        base.Start();
    }

    // Update is called once per frame
    void NewUpdate()
    {
        base.Update();
    }
}
